node_modules
dist
.turbo
*.log
.env
.env.local
.DS_Store
.vite
coverage

# Local deploy secrets — never commit
sweepr.env.local
*.env.local
.claude/
*.tsbuildinfo
apps/marketing/tsconfig.tsbuildinfo

# Vendored agent skills are versioned; the rest of .claude (worktrees,
# settings, session state) stays ignored.
!.claude/
.claude/*
!.claude/skills/
